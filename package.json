{
  "name": "redirect",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "next build && next export -o docs && git checkout -- docs/CNAME && git checkout -- docs/.nojekyll && git add docs && git commit -m \"chore: regen site\" && git push origin main"
  },
  "dependencies": {
    "bulma": "^0.9.3",
    "next": "11.1.2",
    "nextjs-redirect": "^5.0.2",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "sass": "^1.39.0",
    "swr": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.2"
  }
}
